import React from 'react';

import PowerBI from './power-bi';

import AppWrapper from 'app-wrapper';

function PowerBiWrapper() {
  return (
    <AppWrapper>
      <PowerBI />
    </AppWrapper>
  );
}

export default PowerBiWrapper;

PowerBiWrapper.propTypes = {
};