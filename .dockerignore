.git
node_modules
dist
.yarn/cache
.yarn/install-state.gz
.yarn/unplugged

# Generated by the maintenance image's build (docker-publish.yml stages dist/
# into docker/www/chpl and docker/www/error), so it is ~2x the build output and
# never wanted in the live image's context. The maint build itself uses
# `context: docker` and so reads docker/.dockerignore, not this file.
docker/www
