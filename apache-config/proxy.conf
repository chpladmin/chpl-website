LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so

# Example ProxyPass for a backend service named 'my-app'
ProxyPass /rest/ http://host.docker.internal:8181/chpl-service/
ProxyPassReverse /rest/ http://host.docker.internal:8181/chpl-service/
